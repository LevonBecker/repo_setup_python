---
name: Feature Branches

on:
  pull_request:
    branches:
      - development
      - main
    types:
      - opened
      - synchronize
      - reopened

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
